#!/bin/bash
# Managed by Puppet

# Wait up to 600 seconds 
randomsec=$RANDOM
let "randomsec %= 600"
sleep $randomsec

/usr/sbin/ntpdate -s <% scope.lookupvar('ntp::server').each do |server| %><%= server %> <% end %>
# /sbin/clock -w

exit 0
